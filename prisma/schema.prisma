generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model GuildConfig {
  guildId         String   @id
  ipChannelId     String?
  statusChannelId String?
  updatedAt       DateTime @updatedAt
}
